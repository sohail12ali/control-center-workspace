[pytest]
# The console is the only Python in this workspace, so discovery points there
# rather than scanning the vault (which contains .md fixtures, not tests) or
# the sibling repos added as VS Code workspace folders.
testpaths = console/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
