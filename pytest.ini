[pytest]
# The console is stdlib Python; desktop/sidecar.py is also stdlib. Discovery
# stays off the vault (markdown fixtures) and off sibling workspace folders.
testpaths = console/tests desktop/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
