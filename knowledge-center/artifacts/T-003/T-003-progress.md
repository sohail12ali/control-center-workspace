---
ticket: "T-003"
artifact: progress
---

# Progress: T-003

## Status Summary
Stage: CLOSED. VERIFY was clean (`challenge-implementation`, fresh test evidence — 783 pytest, 3/3 cargo test, live smoke — `validate-artifacts`, `reconcile` all clean). T-001 closed this run. T-002 intentionally left open pending user click-through. `close-work T-003` run by `@harness` 2026-09-06; lane `done`, artifact-map row under Completed.

## Dated Log

### 2026-09-06
- Done: kickoff (13 artifacts, ticket.toml + trackers via `kanban.py ticket create`, artifact-map row). Phase 0 smoke recorded in [[T-003-verification]] § Ground truth (before). Fixed `New-FromTemplate.ps1` double-encoding UTF-8 under Windows PowerShell 5.1 (read without `-Encoding UTF8`, wrote with BOM) — rendered T-003 files re-encoded.
- Started: GROUND — analyze next.
- Blocked: —
- Next: `analyze` → `requirements draft` grounded in the approved programme plan.

### 2026-09-06 (cont.)
- Done: GROUND — `analyze T-003` (survey) → [[T-003-analysis]]; `analyze T-003 context` → [[T-003-context-snapshot]]. CLARIFY — `requirements draft` (v0, 10 FRs/12 NFRs/8 BRs, grounded in the approved programme plan + Phase 0 evidence) → `challenge-requirements` (4 findings CR-1..CR-4, 5 gaps G1-G5, 0 blockers) → [[T-003-critique-report]], [[T-003-gap-analysis]] → `requirements enrich codebase` (grounded NFR N/A rows + confirmed GH-runner Rust version via WebFetch) → `requirements iterate` (v0→v1, closed all 4 findings + all 5 gaps, added BR-8) → `requirements freeze` **PASS** (iteration 1, all 10 checklist items ✓) → [[T-003-requirements]] finalized. Decisions recorded in [[T-003-decision-log]] (unconditional subsystem+logger+`--console`, cause-B hygiene framing, single-instance pick+descope, installer deferred, sidecar standalone-import, CI apt-scope, CI Rust-toolchain reliance, serve.log no-rotation).
- Started: —
- Blocked: — (no open questions; single-instance link success remains a build-time unknown, already descope-safe per FR-4/BR-3)
- Next: `@planner` — `requirements T-003 stories` then `plan`/`analyze-components`.

### 2026-09-06 (build)
- Done: TEMPLATE stage started (builder). Task **1b-1** — new `console/server/procs.py` (`CREATE_NO_WINDOW`, `no_window_flags`, `popen_kwargs`), no-op on POSIX, checked at call time (0.5h, est 1h). Task **1b-2** — flags applied at all 6 named spawn sites (`agent_session.py:375,507-512`, `agents.py:209-217`, `agent_tools.py:247-249` keeping `shell=True`+`stdin=DEVNULL`, `onboarding.py:70-71`, `worktrees.py:57-59`); 16 new tests in `console/tests/test_procs.py`, monkeypatching `subprocess.Popen`/`run` and `procs.os.name` to prove both the `nt` and POSIX branch on one machine (1.5h, est 2h). Task **1b-3** — `sidecar.py:124-130` stdout/stderr now redirect to an append handle on `console/.cache/desktop/serve.log` (falls back to `DEVNULL` if the dir can't be made; handle closed in the parent after spawn); `kill_tree`'s taskkill gets its own inline `CREATE_NO_WINDOW` constant, confirmed no import of `procs.py` (standalone-importable preserved); 4 new tests in `desktop/tests/test_sidecar.py` (1h, est 1.5h). `python -m pytest`: 758 → 778 passed, 0 regressions.
- Started: Phase 1 slice 1a (Rust logger + cause-A fix, `logger.rs`/`main.rs`/`tray.rs`).
- Blocked: —
- Next: 1a-1 (`logger.rs`) → 1a-2 (`main.rs` cause-A fix) → 1a-3 (`tray.rs` `log::warn!` wiring) → 1a-4 (README) → 1c-1 (`sidecar.rs::ensure` `extra_env`) → Phase 2 tech-select gate.

### 2026-09-06 (build, cont.)
- Done: Phase 1 slice 1a + 1c complete. Task **1a-1** — new `desktop/src-tauri/src/logger.rs`: `log` crate direct dep (added `features=["std"]`, needed for `set_boxed_logger`/`Box<dyn Log>` — not in the plan doc, discovered at `cargo build`), rotating file writer to `console/.cache/desktop/host.log` (append, rotate to `.1` at 1 MiB, rotation decided before each write so a line is never split across both files), UTC timestamps by hand (`civil_from_days`, no `chrono`). 3 `cargo test`s pass (1.5h, est 2h). Task **1a-2** — `main.rs` `#![windows_subsystem = "windows"]` now unconditional; `--console`/`DESKTOP_CONSOLE=1` → `AttachConsole(ATTACH_PARENT_PROCESS)` else `AllocConsole()`, `#[cfg(windows)]`-gated (no-op stub on other OSes); panic hook logs then calls the existing `alert()`; `find_repo_root` + logger init moved before the Tauri builder; the 3 in-setup fatal paths (`ensure`/`open_window`/`tray::attach` failing) changed from `return Err` (which would propagate through `.expect()` and double-fire the new panic hook) to `log::error!` + `alert()` + `std::process::exit(1)` — a small deliberate fix beyond the literal diff, to avoid a double-MessageBox regression the new panic hook would otherwise cause (1.5h, est 2h). Task **1a-3** — `tray.rs:32-34,45,55,162-163` swallowed `let _ = ...` → `log::warn!` (1h, est 1h). Task **1a-4** — `desktop/README.md` Run section updated for the new `cargo run` behaviour, `--console`, and `host.log` path (0.5h, est 0.5h). Task **1c-1** — `sidecar.rs::ensure` gains `extra_env: &HashMap<String,String>` via `Command::envs`, unused (empty map) by T-003's own call site (0.3h, est 0.5h).
  Evidence: `cargo build --manifest-path desktop/src-tauri/Cargo.toml` clean (debug); `cargo test` 3/3 pass; new `desktop/tests/test_pe_subsystem.py` (added ahead of 3a-1 since 1a-2's own AC needs it) passes against the freshly-built debug exe — subsystem `2`. Live-smoked the debug exe twice (`cmd /c start`): plain launch → `list-console-windows.ps1` reports zero visible console-class windows, `probe-processes.ps1` confirms zero conhost in the host's own process tree, `api/config` answers; `--console` launch → a visible `ConsoleWindowClass` window owned by the host pid itself (AllocConsole fallback, since this launch chain has no attachable parent console). `console/.cache/desktop/host.log` contains real `startup`/`ensure`/`job`/`window`/`tray` lines with correct ISO-UTC timestamps from both launches; `console/.cache/desktop/serve.log` captured the sidecar's stdout. The specific `tray.rs` `log::warn!` sites (show_main/eval_tray/request_quit/desktop-session listener) are code-reviewed but not live-exercised — they only fire on tray-menu clicks, which needs UI automation this pass didn't attempt (same class of limitation as T-002's own tray-menu PENDING row). `python -m pytest`: 778 → 779 passed (the one addition is `test_pe_subsystem.py`), 0 regressions.
- Started: —
- Blocked: —
- Next: Phase 2 — `tech-select T-003 single-instance --mode confirm-existing` (task 2a-1) before any dependency is added.

### 2026-09-06 (build, cont. 2)
- Done: Phase 2 — single-instance guard, complete, no descope needed. Task **2a-1** — `tech-select T-003 single-instance --mode confirm-existing`: the crate/version was already the user-approved programme-plan pick, not a fresh candidate search, so this pass ran the empirical toolchain-link gate rather than web research (no WebFetch/WebSearch tool available to the builder role) — added `tauri-plugin-single-instance = "2"` to `Cargo.toml`, `cargo build` resolved it to `2.4.4` and compiled clean (~48 new transitive crates, mostly `windows-*`/`zbus`), no link failure → no descope. Recorded as `## tech-single-instance-confirm-existing` in [[T-003-decision-log]] (0.4h, est 1h). Task **2a-2** — registered `tauri_plugin_single_instance::init(...)` as the first plugin in the `main.rs` builder chain, callback = `tray::show_main` plus a `log::info!` line (0.5h, est 2h).
  Evidence: live-smoked twice on the debug exe — first launch opens normally; second launch (`cmd /c start` while the first is still running) logs `single-instance: second launch detected, focusing the existing window` in `host.log` and exits immediately; `Get-Process delivery-console-desktop` shows exactly one host process throughout, same pid as before the second attempt. Quit-race (callback firing during shutdown) not separately exercised — no crash observed across the launch/quit cycles run this session, but that specific race wasn't deliberately forced. `cargo build`/`cargo test` (3/3) and `python -m pytest` (779 passed) both re-confirmed clean after the addition.
- Started: Phase 3 (release build & per-OS launch path).
- Blocked: —
- Next: 3a-1 (`cargo build --release`, long-running) → 3b-1/3b-2/3b-3 (per-OS launch scripts) → Phase 4 (CI) → Phase 5 (T-001/T-002 close, verifier/harness territory).

### 2026-09-06 (build, cont. 3)
- Done: Phase 3 complete. Task **3a-1** — `cargo build --release --manifest-path desktop/src-tauri/Cargo.toml` (3m42s). `test_pe_subsystem.py` passes against the release exe (subsystem 2). Phase 0 smoke rows re-verified live on the release build: row 1 (subsystem 2), row 3 (zero visible console/conhost via `list-console-windows.ps1` + `probe-processes.ps1`), row 4 (`api/config` ok), a row-7 equivalent (`[Diagnostics.Process]::CloseMainWindow()` → process stays alive, window hidden, port stays up), a row-8/9 equivalent (force-kill the host → the job object also kills the owned `serve` process, port goes down). Rows 5/6 (agent-chat spawns) and a literal tray-menu Quit click were not automated — need UI automation / configured agent CLI credentials, same class of limitation as T-002's own PENDING tray rows (1.5h, est 2.5h).
  Tasks **3b-1/3b-2/3b-3** — new `desktop/install-shortcut.ps1`, `desktop/launch.ps1`, `desktop/install-launcher.sh`, `.claude/launch.json`, plus `desktop/tests/test_install_launcher.py` (4 new tests). Two real bugs found and fixed while smoke-testing (not just written and assumed correct): (1) `launch.ps1`'s `Start-Process -ArgumentList @()` throws on PowerShell 5.1 when the array is empty — fixed with a splatted hashtable that only adds the key when non-empty, then re-verified live (default launch shows a console, `-NoConsole` doesn't). (2) `test_install_launcher.py` invoking bare `"bash"` via `subprocess.run` resolved to the WSL launcher stub on this Windows machine (`System32\bash.exe`, garbled UTF-16 output) instead of Git Bash, even though `shutil.which("bash")` itself finds Git Bash first — fixed by resolving the full path once and passing that. `install-shortcut.ps1`'s `.lnk`-writing logic verified against a scratch path (COM `WScript.Shell`, correct TargetPath/WorkingDirectory, idempotent overwrite); the real Start-Menu/Desktop/Startup install was **not run** — that writes outside the repo and is ASK-gated, left for the user to run directly (1.7h combined, est 4.5h).
  `python -m pytest`: 779 → 783 passed (4 new `test_install_launcher.py` tests), 0 regressions. One pre-existing, unrelated flake observed and confirmed not caused by this session: `console/tests/test_tomlio.py::test_concurrent_writers_do_not_corrupt` intermittently hits a Windows `os.replace` sharing-violation race (own file untouched this session, confirmed via `git diff --stat`; reproduces ~1-in-3 runs in isolation on a clean stash of this session's changes too).
- Started: Phase 4 (CI).
- Blocked: —
- Next: 4a-1 (`.github/workflows/verify.yml` `desktop` job) → Docs pass (`desktop/README.md`, `console/README.md`) → simplify → hand off Phase 5 (T-001/T-002 close) to the verifier/harness.

### 2026-09-06 (build, cont. 4 — Docs + SIMPLIFY)
- Done: Phase 4 (task 4a-1, `desktop` CI job) landed — see the "cont. 3" entry above for the timing/evidence; recorded here as the phase-close. Docs pass: `desktop/README.md` gained a "Release build & launchers" section (release build, both launcher scripts, `.claude/launch.json`, both log paths) and updated Tests/Layout tables; `console/README.md § Agents tab` (and its duplicate mention under § Config) fixed — backends come from `config/agents.toml`'s `[[backend]]` rows, not a `console.toml` `[agents.backends.*]` table that no longer exists, and "no live steering" is now scoped correctly to the one-shot `agents launch` path (live chats, a separate mechanism, do steer).
  SIMPLIFY (single-pass, Agent tool unavailable — not the full 4-agent fan-out, stated plainly): reviewed all 19 changed files across reuse/simplification/efficiency/altitude. Two real findings fixed: (1) `main.rs` had the same "log + alert + exit" shape inline at 4 call sites (one of them, `find_repo_root`'s failure, actually returned exit code **0** on a real startup failure) — factored into one `fatal(context, msg) -> !` helper, fixing the inconsistency as part of removing the duplication. (2) `tray.rs` had the same "if let Err(e) { log::warn! }" shape at 7 call sites — factored into one `warn_on_err(context, result)` helper. Also: `main.rs`'s fully-qualified `std::collections::HashMap::new()` shortened via a top-level `use`; `test_procs.py`'s `_StubThreading.__getattr__` moved its `import threading` to module scope. Re-verified after refactor: `cargo build`/`cargo test` (3/3) clean, live-smoked the debug exe again (zero console windows, `host.log` lines unchanged in shape), `python -m pytest` 783 passed, release exe rebuilt (40s incremental) to match. No findings skipped — everything flagged was in-scope and fixed.
- Started: —
- Blocked: —
- Next: hand off to `@verifier` — `challenge-implementation` → `verify cases`/`verify ready` → `validate-artifacts` → `reconcile` → `close-work` (Phase 5, T-001/T-002, is the verifier/harness's action, not re-done here).

### 2026-09-06 (verify)
- Done: `challenge-implementation` — no unresolved critical findings; cause-A/cause-B framing confirmed honest in code, docs, and decision-log; Windows-only APIs confirmed `#[cfg(windows)]`-gated (`main.rs:69-88`); `procs.py` confirmed no-op on POSIX; `sidecar.py` confirmed standalone-importable (grep, no `procs` import); CI job confirmed isolated from existing jobs; CI defined-vs-proven distinction maintained. Minor (non-blocking) finding: the pre-existing Windows `tomlio.atomic_write` race the builder called out as localized to `test_tomlio.py` also reproduced once in `console/tests/test_trackers.py::test_status_filter` this pass — same root cause (`tomlio.py`, untouched by T-003's diff), broader manifestation than documented; not a T-003 regression, flagged as a follow-up `todo` candidate, not a blocker.
  Fresh evidence (re-run independently, not trusted from the builder): `python -m pytest` → **783 passed, 0 failed** (`--junit-xml`, deterministic); `cargo test --manifest-path desktop/src-tauri/Cargo.toml` (after `msvc-env.ps1`) → **3/3 passed**; targeted new-test rerun (`test_procs.py`+`test_sidecar.py`+`test_install_launcher.py`+`test_pe_subsystem.py`) → **35/35 passed**. Live smoke against the release exe via `cmd //c start` (matching the ticket's specified method): zero visible console windows on default launch; one `ConsoleWindowClass` window with `--console`; close-to-tray leaves `serve` up (`api/config` 200); second launch = one process + `host.log` single-instance line; force-kill cascades to the owned `serve` process (job object), port 8790 goes down.
  `validate-artifacts` (structure): all 18 T-003 markdown artifacts have `## Links`. (links): FR-1..FR-10 traced to task-breakdown rows to code/tests to verification.md rows — full chain, no unfulfilled requirements found.
  `reconcile`: found `T-003` ticket lane stuck at `open` in `ticket.toml`/`artifact-map.md` despite build being complete — auto-fixed via `kanban.py ticket move T-003 verify` + `artifact-map.md`/`summary.md` sync (markdown only, no TOML hand-edit). `T-003-summary.md` was also stale (still described CANONICAL-stage state) — updated to reflect build-complete/VERIFY.
  T-001: closed (`close-work`) — all 8 ACs PASS, one line added noting the debug-subsystem defect is tracked/fixed here, lane `done`, artifact-map row moved to Completed.
  T-002: one bounded UIA tray-menu drive attempted against the release exe — failed to locate any drivable tray-icon element (stronger negative than T-002's own prior finding). Per instruction, **not closed** — dated "Manual smoke" click-through checklist added to `T-002-verification.md`, PENDING rows relabeled `PENDING — user click-through`.
- Started: —
- Blocked: — (T-002 stays open pending the user's manual click-through; not a T-003 blocker)
- Next: report to harness; harness runs T-003's own `close-work` after reviewing this pass.

### 2026-09-06 (close)
- Done: `@harness` ran `handoff VERIFY→CLOSE` (pass) and `close-work T-003`. Added a decision-log entry recording FR-8 (CI-green-on-3-runners) and FR-10 (T-002 closure) as accepted PENDING, not FAIL — both gated on actions outside this session (an ASK-gated push; the user's manual click-through). `T-003-summary.md` frontmatter set `status: Complete`, `tags: [completed]`, `closed_date: 2026-09-06`, close note appended. `python console/kanban.py ticket move T-003 done` — lane confirmed `Done (terminal)`. `artifact-map.md` row moved from Active to Completed.
- Started: —
- Blocked: —
- Next: `log-work` (author Sohail Ali), then final report to the user. T-002 remains open pending its manual click-through — a follow-up for the user, not a new task.

## Links
- [[T-003-summary]] · [[T-003-analysis]] · [[T-003-requirements]] · [[T-003-critique-report]] · [[T-003-decision-log]] · [[T-003-plan]] · [[T-003-progress]] · [[T-003-verification]]

