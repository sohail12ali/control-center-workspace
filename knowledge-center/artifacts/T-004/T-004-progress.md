---
ticket: "T-004"
artifact: progress
---

# Progress: T-004

## Status Summary
Stage: VERIFY — closed 2026-09-06. Built, tested (949 passed, baseline 783), verified. FR-8 (reply-path speaking) and FR-7 AC2 (Settings-tab control) deferred to T-006 by the 2026-09-06 amendment, not dropped.

## Dated Log

### 2026-09-06
- Done: GROUND — `/analyze T-004` (survey): T-004-analysis.md written, 11 delivery points mapped against current repo state, baseline pytest confirmed green.
- Done: GROUND — `/analyze T-004 context`: T-004-context-snapshot.md written.
- Done: CLARIFY — `/requirements T-004 draft`: T-004-requirements-draft.md filled (v0) — 11 FRs (one per plan delivery point, including the Settings backend picker as its own FR per analysis.md's flagged gap), 8 BRs, 8 NFR rows, 5 data entities, 8 edge cases, 7 interaction rows, 5 external dependencies, 1 stakeholder. §12 Open Questions and §13 Challenge Findings left empty for the next stage.
- Done: CLARIFY — `/challenge-requirements T-004`: 16 ⚠ findings appended to requirements-draft.md §13 (red-team: BR-1 code-review-only proof, FR-8 double-speech mechanism still unnamed, Windows argv-cap-exceeded left as an undecided OR, BR-3 has no testable AC, PS-unavailable likely routine not edge on macOS/Linux, FR-7 settings validation unspecified, `/api/assistant/stream`'s event vocabulary undocumented/unimplemented anywhere in the repo, §9 Interactions undercounts context-snapshot by one row, verb→tool generic exposure not cross-checked, no concurrent-write NFR for `.cache/assistant/*`, no `say` error-result variant, mid-flight backend-change edge case unstated, no palette-UI FR, no secret-in-memory compliance note, `assistant.toml` forward-compat unstated, no future-implementer stakeholder row). T-004-gap-analysis.md populated: 17 gap rows across all 9 categories (4🔴/8🟡/4🟢, was all-zero template). §9 Interactions cross-checked against context-snapshot.md §2 — one row (PS-template-rendering) found missing. Zero blocking questions raised to T-004-questions.toml — every finding is judged resolvable in `requirements enrich`/`iterate` (the plan is prescriptive; no finding required a new stakeholder decision). Draft closer to freeze-ready than needing a real design change: 0 findings invalidate scope/architecture, all are tightening/completeness gaps.
- Blocked: none.
- Next: `/requirements T-004 enrich` to resolve the 16 findings, then `/requirements T-004 iterate` if stakeholder feedback follows, then re-run `/challenge-requirements T-004` before `/requirements T-004 freeze`.
- Done: CLARIFY — `/requirements T-004 enrich`: all 16 §13 findings resolved with cited facts, all 17 gap-analysis rows closed, 0 〈TBD〉 remaining. 2 design decisions recorded (`assistant-chat-identity-flag`, `remember-secret-guard`). FR-8's palette UI scoping call (minimal input+send box, no composer chrome) flagged for stakeholder confirmation rather than silently decided.
- Done: CLARIFY — orchestrator confirmed the FR-8 palette scoping call against plan.md:145 ("the same endpoint from a palette 'Ask assistant' box"); treated as settled.
- Done: CLARIFY — final gate check: verified 0 〈TBD〉, 0 unresolved ⚠ (16/16 have real resolutions), every FR has ≥1 testable AC, every NFR has a concrete target, out-of-scope non-empty, stakeholder sign-off recorded. `T-004-questions.toml` confirmed empty (0 open/blocker questions).
- Done: CLARIFY — `/requirements T-004 freeze`: gate PASSED (10/10 items). `T-004-requirements-draft.md` frontmatter flipped to `frozen` (iteration 0). `T-004-requirements.md` finalized as the canonical frozen artifact (Intent, scope, condensed FR-1..FR-11 with AC checklists, NFR table, data entities, BR-1..BR-9, edge cases, interactions, stakeholders, freeze record). `T-004-iteration-log.md` freeze-attempt row + full pass entry added.
- Done: CLARIFY — `T-004-decision-log.md` completed with the 6 hard-rule decisions (entry-point plugin shape · console-owned persona + `persona_text` second root · kickoff-as-verb, rejecting a full agent turn and a Python template-substitution port · fast-command table scope/BR-1 · Settings backend picker local-first default · memory location + caps under `console/.cache/assistant/`, no vault writes), each grounded in plan.md § "T-004 — Assistant brain," in addition to the 2 pre-existing challenge-resolution decisions.
- Blocked: none.
- Next: hand off to `@planner` — `requirements T-004 stories`, then `plan`/`analyze-components` (CANONICAL). This ticket (analyst) does not touch `T-004-plan.md`.
- Done: CANONICAL — `analyze-components T-004`: `T-004-components.md` written, 12 components across 3 layers, dependency graph (no cycles), C1 flagged bottleneck (7 of 11 other components sit behind it), C5/C9 flagged novel/no-in-repo-analogue risk concentrations, 3-phase safe build order.
- Done: CANONICAL — `breakdown-tasks T-004`: `T-004-task-breakdown.md` (41 atomic tasks / 3 phases / 11 slices) and `T-004-implementation-plan.md` (phase/slice narrative + AC coverage) written.
- Done: CANONICAL — `estimate T-004 --mode upfront`: `T-004-effort-estimate.md` written — 47.5h Dev / 85.0h Final-Complete [57.0-98.2h], diverging ≈2.0x (Dev) / ≈3.5x (Final) from the source programme plan's pre-decomposition "M · 3 d" sizing, driven by C5/C9's ×1.8 QC cycle-factor penalty.
- Amended: reconciled the 47.5h/85.0h divergence with the user before any build started; user chose to trim scope rather than force-fit the estimate. **Amended requirements**: run per `evolve` (target: requirements, reason: user-directed scope change) — see [[T-004-decision-log]] § Amendment 2026-09-06 for the full before/after. FR-8 (C9: reply path, `is_assistant` flag, `assistant.js`, `spoken_form()`) deferred in full to T-006; FR-7 AC2 (C7's Settings-tab UI picker) deferred to T-006, C7's service half stays in T-004. 2 new FR-6 ACs added for defects observed this session (artifact-map `## Active` insertion point; `New-FromTemplate.ps1` double-encoding regression guard, script fix already applied and T-003/T-004 files re-encoded). Cascaded: `T-004-components.md` (C9 row, C7 purpose text, dependency graph, safe-build-order, status summary), `T-004-task-breakdown.md` (Slice 8 removed, task 3-7-3 removed, tasks 2-6-6/2-6-7 added, is_assistant kwarg stripped from 1-2-1/1-2-3/1-2-6, phase subtotals and effort summary recomputed: 36 tasks/40.5h, was 41/47.5h), `T-004-implementation-plan.md` (same numbers, ticket summary, dependency graph, AC coverage), `T-004-effort-estimate.md` (revision-log row flagging the 85.0h Final/Complete figure stale, not recomputed — needs `estimate(mode=forecast)` before scheduling).
- Blocked: none.
- Next: hand off to `@builder` — Phase 1 first (Slices 1/2/9/11: C0, C1, C8, C10 — C1 is the priority within Phase 1, everything in Phase 2 stalls until it lands), `progress-tracker` per task.

### 2026-09-06 (builder)
- Done: TEMPLATE — Phase 1, Slice 2, **C1 (bottleneck)** built first as required, tasks 1-2-1..1-2-6 (est. 5.5h): `BaseSession.__init__`/`agent_session.build()` gain `system_append=""`/`extra=""` kwargs (`console/server/agent_session.py`); `Backend.session_argv` gains `system_append=""` threaded into `_expand`, plus a new `supports_system_append_flag` property that reads the capability off the backend's own argv templates rather than hardcoding backend ids (`console/server/agent_backends.py`); claude's `agents.toml` row gains `--append-system-prompt {system_append}`; `LiveSession.start()` passes `system_append=self.system_append` to `session_argv`; `ApiSession.start()` passes `extra=self.extra` to `prompt_build.build(...)` (`prompt_build.build` already accepted `extra=`, unused until now); `agent_manager.create(...)` gains `system_append=""`/`extra=""`, passed to `agent_session.build(...)`, and prepends `system_append` to the first-turn wire only when `not backend.supports_system_append_flag` (the cursor-agent-shaped path) — claude's flag carries it instead, wire untouched. Tests: extended `test_api_session.py` (extra/system_append defaults + `extra` reaching the system message), `test_agent_backends.py` (new `TestSystemAppend` class — flag present/absent, unaffected row, `supports_system_append_flag` on a synthetic row and the shipped claude row), new `console/tests/test_assistant.py` (`TestSystemAppendDispatch` — fake cursor-agent vs claude backends via a stubbed `agent_session.build`, proving the wire-prefix branch without spawning a real process). All new/extended tests green; full suite still 783 passed (dot-count verified — see note below on the pre-existing missing summary line).
- Note: `python -m pytest` prints all dots but the trailing "`N passed in Xs`" summary line never appears and the process exits 0 immediately after the last dot, on this Windows/Python 3.14 environment — pre-existing, unrelated to this change (confirmed on the untouched baseline via dot-count = 783, matching the stated baseline exactly). Verified pass/fail by dot-count and absence of `F`/`E` markers, not by the summary line.
- Blocked: none.
- Next: Phase 1 continues — C0 (Slice 1, `audit.py` ACTIONS extension + 7-agent structural test), then C8 (Slice 9, `assistant.py` memory), then C10 (Slice 11, `native_bridge.py` stub).

### 2026-09-06 (builder, continued)
- Done: TEMPLATE — Phase 1, Slice 1, **C0** (tasks 1-1-1, 1-1-2, est. 1h): `audit.ACTIONS` extended with `assistant.say`/`kickoff`/`remember`/`settings`/`persona_truncated` (`console/server/audit.py`); real-repo structural test added to `test_harness_lint.py` (`TestRealAgentRoster`) asserting `.claude/agents/*.md` stays at exactly 7 files, reading the actual workspace directory rather than a fixture. `test_notify_audit.py` extended with a parametrized test over all 5 new action names.
- Done: TEMPLATE — Phase 1, Slice 9, **C8** (tasks 1-9-1..3, est. 3h): new `console/server/assistant.py` — `session.json` pointer (create/read/overwrite, `created_at` survives a same-sid overwrite, a corrupt file is treated as absent), capped `memory.md` (≤1,500 chars, oldest-first trim keeping the newest fact, never errors on overflow) with the secret-shaped-fact guard (PEM blocks, `sk-`/`sk-ant-`/`AKIA`/`ghp_`/`xox*`/`AIza` key prefixes, a bare `KEY=value` line — verified not to false-positive on an ordinary sentence containing `=`), and a last-reply file that overwrites per turn. New `console/tests/test_assistant.py` grown with `TestSessionPointer`/`TestMemory`/`TestLastReply` (22 tests total in that file so far, all green).
- Done: TEMPLATE — Phase 1, Slice 11, **C10** (task 1-11-1, est. 1h): new `console/server/native_bridge.py` — reads `console/.cache/desktop/bridge.json` (T-005's future pointer file); `available()` probes a `base_url` if the pointer names one (forward-compatible design, mirrors `agent_backends._probe`'s reasoning), and reports `(False, "shell not running")` whenever the file is absent, corrupt, or names nothing reachable — which is T-004's actual, honest state, since nothing yet writes that file. Two per-endpoint helpers, `clipboard_read` (5s) and `screenshot` (60s), both degrade to `{"ok": False, "reason": ...}` rather than raising. New `console/tests/test_native_bridge.py` (10 tests, fake-opener idiom matching `test_api_session.py`), all green.
- **Phase 1 complete** (12/12 tasks, all 4 components C0/C1/C8/C10 done). Full suite re-run: 830 passed (was 783 baseline — 47 new tests added across Phase 1's 4 slices), 0 failed/errored (verified by dot-count and absence of `F`/`E`, per the pre-existing missing-summary-line note above).
- Blocked: none.
- Next: Phase 2 — Slice 1/C2 (`assistant_feature.py` plugin + routes), building on C0+C1; then Slice 3/C3 (persona second root); then Slice 6/C6 (new verbs).

### 2026-09-06 (builder, continued 2)
- Done: TEMPLATE — Phase 2, Slice 1, **C2** (tasks 2-1-1..5, est. 7h): new `console/server/features/assistant_feature.py` (`say`/`session`/`new`/`stream`/`memory` routes, tenth `plugins.toml` row) — `say` reuses the one Assistant chat (session pointer via C8), catches a backend failure to `{"result":"error","reason":...}` rather than a 500, and is `audit.record`-wrapped (BR-2); `new`/`session` reuse-or-recreate on idle timeout (a bare `DEFAULT_IDLE_MINUTES=240` constant stands in for C7's `assistant.toml`, which task 3-7-4 will read fresh per `new`); `stream` filters to exactly the 5 named event types via a new additive `types=` kwarg on `agent_events.Stream.subscribe`/`agent_manager.subscribe` (default `None`, fully backward compatible — `agents_feature.chat_stream`'s own call site is unaffected) rather than a second event log; `memory` GET/POST wrap `assistant.py`'s capped store, including the secret-shaped-fact guard reaching the route. `httpd.py` and `app.js` untouched, confirmed. Tests: new `test_assistant.py` classes (`TestSayRoute`, `TestSessionAndNewRoutes`, `TestStreamRoute`, `TestMemoryRoutes`, 14 tests, all against a fake `agent_manager` — no real backend process spawned), `test_plugins.py` extended (plugin-enabled check, no-tab check, real route-registration-set check against the real checkout). Full suite: 847 passed (was 830), 0 failed.
- Blocked: none.
- Next: Phase 2 continues — Slice 3/C3 (persona + `persona_text` second root), then Slice 6/C6 (new verbs: `kickoff`/`tickets_digest`/`remember`).

### 2026-09-06 (builder, continued 3)
- Done: TEMPLATE — Phase 2, Slice 3, **C3** (tasks 2-3-1..3, est. 3h): new `console/config/assistant.md` persona (2,513 bytes — role, reply contract, tool preferences, safety list incl. the remember-secret rule, known fast-command list); `prompt_build.persona_text` gains a console-owned second root (`console/config/`, tried before `.claude/agents/%s.md`, existing mechanism unaffected) and a `PERSONA_CAP=4000` enforcement with a stated truncation marker + `audit.record("assistant.persona_truncated", ...)`, both inside `persona_text()` itself so every caller inherits the guarantee. Also wired the per-backend routing this second root makes meaningful: new `assistant_feature._persona_kwargs` sends `persona="assistant"` to an `openai_api` backend (which reads persona text itself via `prompt_build.build`) and `system_append=persona_text(...)` to every other transport, since a CLI's own `--agent`-style flag expects a `.claude/agents/` id the console-owned persona deliberately isn't (BR-3) — `_ensure_session` now calls this before `agent_manager.create`. Tests: `test_prompt_build.py` (`TestConsoleOwnedPersonaSecondRoot`, `TestPersonaCap` — incl. the audit-call assertion), `test_assistant.py` (`TestPersonaRouting`). Full suite: 855 passed (was 847), 0 failed.
- Blocked: none.
- Next: Phase 2 finishes with Slice 6/C6 (new verbs: `kickoff`/`tickets_digest`/`remember`), the largest remaining Phase 2 slice (8h) — then Phase 3.

### 2026-09-06 (builder, continued 4)
- Done: TEMPLATE — Phase 2, Slice 6, **C6** (tasks 2-6-1..7, est. 8h): new `console/server/kickoff.py` — `next_ticket_id` (scans `knowledge-center/artifacts/` for the highest existing `{prefix}NNN}`), `create_ticket` (the kickoff skill's 3 steps: `tickets.create`, render every `_template/*.md` via the real `New-FromTemplate.ps1` through a `runner=` seam, append the artifact-map row), `_insert_under_active` (heading-relative insertion — the 2026-09-06 amendment's regression fix). `PowerShellUnavailable(RuntimeError)` is its own type so C5 can catch exactly it later. New `context.tickets_digest` (composed from `tickets`/`boards`/`trackers`, same readers the per-ticket digest already uses, capped at 1,200 chars, truncation stated) backs the new `tickets_digest` verb; `remember` verb wraps `assistant.remember` (secret guard inherited for free). 3 new `verbs.toml` rows (`kickoff`/`tickets-digest`/`remember`; `kickoff` and `remember` both `needs_confirm=true` as mutating writes) + `verb_handlers.py` one-line adapters. Tests: new `test_kickoff.py` (13 tests — `next_ticket_id`, `create_ticket` incl. the `PowerShellUnavailable` honest-failure path, `TestArtifactMapInsertion`'s 4 tests directly proving the amendment's regression — including the exact "Completed non-empty, Active empty" shape — and `TestRenderedTemplateEncoding`, which copies the REAL `New-FromTemplate.ps1` + `Get-RepoRoot.ps1` into a throwaway repo and actually runs PowerShell against real mis-encoded punctuation, byte-level BOM/UTF-8 assertions, honest skip off Windows), extended `test_verbs.py` (`TestT004Verbs`, 6 tests, gates + secret-guard-still-applies-via-verb). **Phase 2 complete** (15/15 tasks, all 3 components C2/C3/C6 done). Full suite: 874 passed (was 855), 0 failed.
- Blocked: none.
- Next: Phase 3 — Slice 4/C4 (injected session context: tickets digest + memory + capabilities into `extra=`), then Slice 5/C5 (fast-command dispatch table — the ticket's remaining novel-risk component), then Slice 7/C7 (Settings backend picker, service half), then Slice 10/C11 (`kanban.py assistant say` CLI).

- Done: TEMPLATE — Phase 3 completed by the orchestrator directly after three delegated build runs stalled or mis-reported (one stopped after amending plan artifacts, one reported "build not dispatched" while its own builder had in fact written Phase 1-2, one stalled on a watchdog after C4). Ground truth was re-established from the working tree and the test count each time rather than from an agent's status line. Built: **C5** `console/server/assistant_commands.py` — pure matcher (normalise + wake-word strip + spoken-ticket canonicalisation + an 11-row whole-utterance table returning a `Command` description the caller executes, so the table is testable with no backend); **C7 service half** `console/server/assistant_config.py` + `console/config/assistant.toml` — committed defaults merged with a gitignored per-machine override (`console/.cache/assistant/settings.json`), validated-whole writes, local-first backend resolution, plus `GET`/`POST /api/assistant/settings`; **C11** `kanban assistant say|session|memory|settings`, sharing the route's own handler via a new `assistant_feature.handlers()` capture-ctx seam so there is one implementation and no CSRF bypass. Also wired the table into `say` as its single branch point, added `assistant.clear_session`, and added `native_bridge.clipboard_write` (POST support in `_call`) so the "copy that" row calls a function that exists.
- Done: SIMPLIFY — two defects found by the new tests and fixed: (1) `status t dash two` fell through to the model because the ticket span was enumerated in the regex; replaced with a loose capture validated by `canonical_ticket`, which is what the module docstring already claimed. (2) `_h_status` treated `context.build`'s `plan["open"]` as a count when it is a capped LIST; open count is now `total - done`. Also dropped two dead stub methods from the capture ctx (they tripped the shipped-registry test that asserts this file never mentions tab registration) and moved a comment that my own patch had orphaned.
- Done: VERIFY — `python -m pytest -o addopts="" -q` **949 passed** (baseline 783; 166 new). Live deterministic CLI checks pasted verbatim into [[T-004-verification]]; live model turn over HTTP on :8791 proved the persona+context path (the reply described the harness from the injected context) and the server was stopped afterwards. `console/README.md` gained a `## The Assistant` section. FR-8 and FR-7 AC2 recorded as deferred to T-006, not failed.
- Blocked: none.
- Next: `close-work T-004`, then T-005 (native bridge + tray icon states + screenshot/OCR/clipboard).

### Honest note on the FR-8 deferral

`copy that` cannot succeed in T-004 even with a bridge running: nothing calls
`assistant.write_last_reply`, because its only caller is the reply watcher that
the amendment moved to T-006. The function is written and tested but has no
producer yet, so the command answers "no last reply to copy yet" rather than
appearing to work. Consequence of the descope, recorded rather than papered over.

## Links
- [[T-004-summary]] · [[T-004-analysis]] · [[T-004-requirements]] · [[T-004-decision-log]] · [[T-004-plan]] · [[T-004-progress]] · [[T-004-verification]]
