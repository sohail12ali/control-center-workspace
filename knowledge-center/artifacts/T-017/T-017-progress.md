---
ticket: "T-017"
artifact: progress
---

# Progress: T-017

## Status Summary
Stage: TEMPLATE — all 4 phases complete (58/58 tasks, 1357 tests passing). Build ready for handoff(builder → verifier).

## Dated Log

### 2026-09-16
- Done: GROUND — `analyze` (survey + context) grounded against actual code (mcp.py, paths.py, kanban.py, verbs.toml, plugins/registry.py, vault.py, agent_manager.py, kickoff.py, runs.py, trackers.py); CLARIFY — `requirements draft` → `challenge-requirements` (0 blockers, 6 gaps all resolved as documented assumptions) → `requirements freeze` (frozen). One ⚠ (UI EventSource live-sync ambiguity vs. plan's mermaid diagram) escalated to user; user confirmed out-of-scope for T-017, recorded in requirements.md Out-of-Scope + decision-log.md a8.
- Done: CANONICAL — `requirements stories` (8 user stories, US-1..US-8, all 11 FRs covered) → `analyze-components` (15 components, 4 layers, dependency graph, 2 near-tied ~32h critical-path chains) → `plan risk` (6 risks scored, 0 high×high unmitigated) → `estimate(mode=upfront)` (140h dev / 235.2h Final-Complete envelope, components basis, Medium confidence) → `breakdown-tasks` (58 tasks across 4 phases, 126.5h dev, synthesized into implementation-plan.md) → `challenge-plan` (5 findings: 0 critical / 2 major / 3 minor; both major findings fixed in place — NFR-5 audit-coverage gap closed with 2 new tasks, critical-path recomputed from task-dependency weights; 3 minor findings accepted with rationale). Gate clear.
- Blocked: none.
- Next: handoff(planner → builder); `@builder` starts Phase 1 (all 5 slices — one-api, tracker-spi interface, workspace-contract schema, ready-claim-hooks data schema, mcp-first-class resources — are parallelizable, no intra-phase dependency).

Stage: CLARIFY → CANONICAL → TEMPLATE (pending builder)

### 2026-09-16 (builder, Phase 1)
- Board-lane sync: `python console/kanban.py ticket move T-017 in-progress` — ticket.toml stage moved `open` → `in-progress` (was never advanced past kickoff despite GROUND/CLARIFY/CANONICAL being done).
- Done: Slice 1d — ready-claim-hooks (data schema) [1d-1..1d-6], 6h actual vs 8h estimated. `console/server/tickets.py` gains `claimed_by`/`claimed_at` fields (default `""`, back-filled via `setdefault` for older ticket.toml) + `tickets.set_claim(repo_root, ticket_id, claimed_by, claimed_at=None)` mutator, distinct from `owner`/`set_field`. `console/server/trackers.py` gains `comments` as a 4th `VALID_KINDS` entry (id format `C{n}`, default status `open` — matches existing kinds' invariant rather than inventing a `posted` status; never a blocker, mirrors `todos`). `{T}-comments.toml` scaffolding is automatic via the existing `ensure_all()` loop over `VALID_KINDS` — no new scaffolding code needed (task 1d-5 folded into 1d-4). `console/README.md` CLI usage + data-model section updated for both. Evidence: `pytest -o addopts="" console/tests/test_tickets.py console/tests/test_trackers.py console/tests/test_kickoff.py -q` → 77 passed.
- Deviation: decision-log a3 says "`tickets.set_field` (or a dedicated claim/release path) is the only mutator" — chose the dedicated `set_claim` path only; did not additionally restrict `set_field` from touching `claimed_by`/`claimed_at`, since `set_field`'s existing guard (`if field not in ticket`) is already this permissive for other non-`EDITABLE` fields (e.g. `id`) — a pre-existing gap outside this task's scope, not introduced or worsened here.
- Done: Slice 1c — workspace-contract (schema) [1c-1..1c-3], 5h actual vs 5h estimated. New `console/server/workspace_config.py`: `find`/`parse`/`resolve` for an optional `workspace.toml` (`name`/`vault`/`console`/`projects`), raising a named `WorkspaceConfigError` for a missing key or an unresolvable `vault`/`console` path rather than a bare `FileNotFoundError`. Schema/parsing only per the plan — no wiring into `paths.find_repo_root` yet (that's Phase 2's 2c). Evidence: `pytest -o addopts="" console/tests/test_workspace_config.py -q` → 10 passed.
- Done: Slice 1b — tracker-spi (interface) [1b-1..1b-4], 4h actual vs 8h estimated. New `console/server/backends/{__init__.py,base.py}`: `Backend(abc.ABC)` defining `list`/`show`/`create`/`move`/`set`/`comment`/`ready`/`claim` as `@abstractmethod`s (mirrors `plugins/base.py`'s "one tiny contract" shape per a4, distinct from both `plugins/registry.py` and `trackers.py` — docstring cites a4 directly). `verb_handlers.py` gains a stub import (`from . import backends as backends_mod`) noting the future call sites (`ticket_move` now direct, `ready`/`claim`/`comment` in Phase 3) — no behavior change, `VaultBackend` (the one adapter) lands in Phase 2's 2b. Evidence: `pytest -o addopts="" console/tests/test_backends.py console/tests/test_ui_endpoints.py console/tests/test_mutation_verbs.py -q` → 63 passed; full suite `pytest -o addopts="" console/tests -q` → 1237 passed (no regressions from the new import).
- Done: Slice 1a — one-api [1a-1..1a-7], 8.5h actual vs 9.5h estimated. **1a-1 audit finding (deviation):** the plan's named "mixed subcommands" (`verb list`, `audit`, `context`, `telemetry`, `schedule list/due`, `job list`, `worktree list`, `harness lint`, `agents models/provider/doctor`) already had `--json` in current code — reading `build_parser()` end to end found the real gap was 3 subcommands with no JSON path at all: `export`, `reset`, `notify who`. **1a-2/1a-3:** all three now take `--json` (`reset --json` without `--yes`/`--dry-run` refuses by name rather than hanging on an `input()` prompt with nowhere to render); `console/tests/test_cli_json.py` (7 cases) documents the full audit and covers the 3 fixed subcommands. **1a-4/1a-5:** `kickoff.create_ticket` gained optional `ticket_id`/`priority`/`url` kwargs so `kanban.py ticket create ID --title T` (explicit id) and `verb run kickoff` (allocated id) share one path; `cmd_ticket_create` now calls it, so the CLI produces all 3 kickoff artifacts (ticket.toml, rendered templates, artifact-map row), not just a bare `tickets.create` — the old shortcut is no longer reachable from the CLI, `tickets.create` remains only as the internal helper `kickoff.create_ticket` itself calls. **1a-6:** `kanban.py main()` now also catches `kickoff.PowerShellUnavailable` for a clean `error:` line, not a traceback; MCP (`mcp.call_tool`) and HTTP (`httpd._dispatch`) already had generic catch-alls pre-dating this ticket — verified by reading, not modified. **1a-7:** `audit.record("ticket.create", ...)` is called once inside `kickoff.create_ticket` itself (new `"ticket.create"` row in `audit.ACTIONS`) so every entry point (CLI, verb, MCP, HTTP) is audited from one place, closing the CR-1 NFR-5 gap. Evidence: `pytest -o addopts="" console/tests/test_cli_json.py console/tests/test_ticket_create_collapse.py console/tests/test_kickoff.py console/tests/test_tickets.py console/tests/test_mutation_verbs.py console/tests/test_notify_audit.py -q` → 130 passed; full suite `pytest -o addopts="" console/tests -q` → 1251 passed.

- Done: Slice 1e — mcp-first-class (resources) [1e-1..1e-6], 8.5h actual vs 17h estimated (highest-uncertainty slice, per plan). `mcp.py`'s `initialize` capabilities gain `resources: {subscribe: true, listChanged: false}` (additive — existing `tools/list`/`tools/call` behavior verified unchanged). New `resource_list`/`resource_read` functions: one `ticket://{ID}` resource per ticket; `resources/read` delegates to `context.build`/`format_markdown` (verified byte-identical to the `context` tool's own output, not a second representation). Greenfield **1e-4**: new `console/server/bus.py` — a minimal `Bus` (subscribe/publish/drain), pull-not-push because the stdio transport's `serve_forever` has no idle-connection thread to push down mid-wait; a session's own next `handle()` turn drains and forwards via `notifications/resources/updated` (1e-5, plus a symmetric `resources/unsubscribe`). Wired into the one pre-existing mutating verb this phase touches, `verb_handlers.ticket_move` (`ready`/`claim`/`comment` will publish the same way once Phase 3 lands them) — kept strictly to MCP resource subscribers, no UI EventSource/WebSocket code added (decision-log a8). Evidence: `pytest -o addopts="" console/tests/test_mcp.py console/tests/test_bus.py -q` → 44 passed (includes 2 pre-existing `test_mcp.py` assertions updated for the new additive capability — `test_only_implemented_capabilities_are_declared` and `test_unknown_method_is_a_jsonrpc_error`, both correctly changed by this task, not weakened); full suite `pytest -o addopts="" console/tests -q` → 1270 passed.

**Phase 1 complete: all 5 slices (1a/1b/1c/1d/1e) done.** Actuals: 1a 8.5h, 1b 4h, 1c 5h, 1d 6h, 1e 8.5h = **32h actual vs 55.5h estimated** (42% under). No `evolve`/`replan` triggered — every deviation was either a task folding into an adjacent one once the real code was read (e.g. 1d-5, 1b-2, 1a-5, 1e-1 fully) or genuinely faster once grounded (1e-4's uncertainty was in shape, not volume). No scope was dropped; every task's stated acceptance criterion is met and tested. Full test suite green throughout (1270 passed at Phase 1 close). Stopping here per the per-phase checkpoint policy — Phase 2 (Adapters & transport, 41h) awaits review.

Stage: TEMPLATE (Phase 1 of 4 complete, builder checkpoint)

### 2026-09-16 (builder, Phase 2)
- Done: Slice 2c — workspace-contract (resolution) [2c-1..2c-4], 3h actual vs 8h estimated. `console/server/paths.py`'s `find_repo_root` gains a first branch that tries `workspace_config.resolve()` on every search candidate before falling through, unchanged, to the pre-existing sibling-folder walk (a7: `resolve()` returns `None`, never raises, when no `workspace.toml` exists — confirmed no-op via `test_paths.py::test_the_real_checkout_still_resolves`, which checks this repo's own real checkout, and via the full suite staying at 1270 passed before this slice's new tests were even added).
  - **Deviation, flagged (not silently narrowed):** the plan's own illustrative `workspace.toml` (`vault = "../noble-knowledge"`, an arbitrarily *renamed* vault directory) cannot be reduced to the single `repo_root` string every other module in `console/server/` still assumes (~20 call sites join `repo_root + "console/..."` / `"knowledge-center/..."` literally — `boards.py`, `agent_backends.py`, `audit.py`, `runs.py`, etc. — none of which Phase 2's task list touches). Implemented the subset this 8h/paths.py-only slice can actually deliver without a much larger multi-module refactor: `find_repo_root`'s `workspace.toml` branch resolves correctly when `vault`/`console` are literally named `knowledge-center`/`console` and share one parent (letting that parent be found anywhere via `workspace.toml`, not just by walking up from cwd) — matching the requirements-draft's own literal FR-6 AC wording ("a test fixture with `console/` and `knowledge-center/` in separate directories"). A `workspace.toml` naming a genuinely renamed/relocated pair raises a named `RepoRootError` rather than silently resolving to a wrong root or crashing deep in an unrelated module. Full arbitrary-rename support (the plan's aspirational example) needs those ~20 call sites to take a resolved root instead of a literal join — real future work, not something to guess at or half-build here; flagging for `@planner`/`evolve` if the full vision is still wanted, since it's a genuinely different-sized change than an 8h slice.
  - Evidence: new `console/tests/test_paths.py` (6 cases: plain-sibling resolve, upward walk, the real checkout as a no-workspace.toml pin, a split layout `workspace.toml` resolves, an unreducible layout raises, a broken `vault`/`console` path still raises `WorkspaceConfigError`); `pytest -o addopts="" console/tests/test_paths.py console/tests/test_workspace_config.py -q` → 16 passed.
- Done: Slice 2b — tracker-spi (vault adapter) [2b-1..2b-7], 6.5h actual vs 17h estimated. New `console/server/backends/vault_backend.py`: `VaultBackend` — thin delegation to `tickets.py`/`trackers.py` for `list`/`show`/`create`/`move`/`set`/`comment`; `ready` (unblocked + unclaimed, reading the same `trackers.blockers` the board card already uses plus 1d-1's `claimed_by`) and `claim` (delegates to `tickets.set_claim`, deliberately last-write-wins — race-safety is Phase 3's 3a-5, not this slice) are real logic rather than passthroughs since Phase 3's verb handlers need something to call. Registered as the process-wide singleton `backends.default_backend()` (mirrors `bus.default()`'s shape) — the only adapter (2b-3/2b-6: no Jira/Azure/Linear/GitHub Issues *class* exists anywhere in the package, checked by a test that inspects class definitions rather than the docstrings' own mentions of them as future-only possibilities). `verb_handlers.ticket_move` (2b-4) now calls `backends_mod.default_backend().move(...)` instead of `tickets_mod.move(...)` directly — the one pre-existing mutating verb this phase rewires, matching 1b-3's stub comment. 2b-7's design note (future non-vault adapter syncs alongside the vault, never replaces it) is in `vault_backend.py`'s module docstring. Evidence: new `console/tests/test_vault_backend.py` (13 cases, round-tripping all 8 methods against the real `repo` fixture, plus the ticket_move-routes-through-the-backend spy test and the no-other-adapter check); `pytest -o addopts="" console/tests/test_vault_backend.py console/tests/test_backends.py console/tests/test_mutation_verbs.py console/tests/test_ui_endpoints.py -q` → 75 passed.
- Done: Slice 2a — mcp-first-class (HTTP transport) [2a-1..2a-5], 7h actual vs 16h estimated. New `console/server/features/mcp_http_feature.py`, a no-tab plugin (registered in `console/config/plugins.toml`, matching `verbs_feature.py`'s shape) mounting `POST /api/mcp` (request/response) and `GET /api/mcp/sse` (server-initiated messages) on the same process/port `serve` already runs — stdio (`console/mcp_server.py`) untouched. **2a-3 (share logic, no duplication):** the HTTP handler builds one `mcp.Server` per session and calls its exact `handle()` — the same method stdio's loop calls — against an in-memory `io.StringIO` standing in for stdout, then parses back whatever it wrote; `initialize`/`tools/list`/`tools/call`/`resources/*` have exactly one implementation. Sessions persist across HTTP requests in a process-local dict, keyed by an id the transport hands back on `initialize` and a client resends via the `Mcp-Session-Id` header (`httpd.Request` gained this one named field, alongside the existing `user_agent`/`client_addr` — still narrow, not a general headers dict) or, for the SSE `GET` a native browser `EventSource` can't attach headers to, a `?session=` query parameter.
  - **Bug found and fixed while wiring 2a-3/2a-2 together, not left in:** `mcp.Server.__init__` unconditionally set `self.session_id = id(self)` (a Python object id), which is what `resources/subscribe` and the bus key subscriptions by — but the HTTP transport's own client-visible session id is a separate uuid string. Without a fix, `resources/subscribe` over HTTP would key the bus by an id no HTTP request ever sees again, and `GET /api/mcp/sse`'s drain-by-uuid would silently never find anything (a real hang was caught by the test suite itself — `TestSseNotifications` looped forever against an empty queue before the fix). Fixed by giving `Server.__init__` an optional `session_id=` parameter (default unchanged: `id(self)` when not given, so stdio's behavior is untouched) and passing the HTTP transport's own session id through it — one shared identity, not two.
  - Evidence: new `console/tests/test_mcp_http.py` (9 cases: routes exist, `initialize` returns a session id and the FR-3 `resources` capability, `tools/list`/`tools/call` byte-identical over HTTP vs. a real stdio `Server` instance (2a-4), a session persisting across requests, an unknown session id starting a fresh session rather than erroring, a subscribed session receiving a mutation notification over SSE (2a-2), an unknown SSE session failing cleanly, and two concurrent sessions never seeing each other's notifications with one's stream close not disturbing the other's registry entry (2a-5)); `pytest -o addopts="" console/tests/test_mcp_http.py -q` → 9 passed.
- Full suite after all of Phase 2: `pytest -o addopts="" console/tests -q` → **1298 passed** (1270 at Phase 1 close + 28 new: 6 `test_paths.py` + 13 `test_vault_backend.py` + 9 `test_mcp_http.py`; `test_workspace_config.py`'s existing 10 unaffected). One `PytestUnhandledThreadExceptionWarning` from `test_tomlio.py`'s pre-existing concurrent-writer stress test (a Windows file-lock race unrelated to any Phase 2 change) surfaced non-deterministically on the full-suite run; re-ran `test_tomlio.py` alone immediately after — 16 passed, confirming it is a known-flaky pre-existing test, not a regression.

**Phase 2 complete: all 3 slices (2a/2b/2c) done.** Actuals: 2c 3h, 2b 6.5h, 2a 7h = **16.5h actual vs 41h estimated** (60% under — mirrors Phase 1's pattern of the components/adapters being smaller once the real code was read, plus reusing `mcp.Server.handle()` directly for 2a instead of writing a second protocol layer). One flagged scope gap (2c: `workspace.toml` support for a *renamed* vault/console pair, per the plan's own illustrative example, is not fully realized — see the 2c note above) and one real bug found-and-fixed in the same slice it was introduced in (2a's session-id mismatch) — neither is a silent scope change: both are called out here and in task-breakdown.md's Notes column. No other deviations. Stopping here per the per-phase checkpoint policy — Phase 3 (Verbs, 18h) awaits review; not starting it without the harness orchestrator's go-ahead.

- `simplify` pass (single-pass, no Agent-tool fan-out available in this context) over the full Phase 2 diff (`paths.py`, `backends/vault_backend.py`, `backends/__init__.py`, `verb_handlers.py`, `mcp.py`, `httpd.py`, `mcp_http_feature.py`, `plugins.toml`, `test_paths.py`, `test_vault_backend.py`, `test_mcp_http.py`) across reuse/simplification/efficiency/altitude: no findings beyond the session-id bug already fixed inline while building 2a (see above) — no further changes applied.

Stage: TEMPLATE (Phase 2 of 4 complete, builder checkpoint)

### 2026-09-16 (builder, Phase 3)
- Done: Slice 3a — ready-claim-hooks (verb handlers) [3a-1..3a-12], 6.5h actual vs 18h estimated. Three new verb handlers in `verb_handlers.py` (`ticket_ready`, `ticket_claim`, `ticket_comment`), three `[[verb]]` rows in `verbs.toml` (`ready`, `claim`, `comment`), two new `audit.ACTIONS` entries (`ticket.claim`, `ticket.comment`).
  - **FR-7 (`ready`, 3a-1..3a-3):** thin wrapper over `backends.default_backend().ready(...)` (2b-2's real filter logic — unblocked via `trackers.blockers`, unclaimed via `claimed_by`). No `needs_ticket`/`needs_confirm` gate — a workspace-wide read. **One-api confirmed, not re-plumbed (3a-2):** reading `mcp.call_tool`, `verbs_feature.py`'s HTTP route, and `kanban.cmd_verb_run` end to end confirmed all three already dispatch through `verbs.run`'s shared registry (Phase 1's 1a/1b) — adding the verb to `verbs.toml` was the entire task, no per-surface code.
  - **FR-8 (`claim`, 3a-4..3a-8), the real work of this slice:** `claim` needs an agent identity via the existing generic `--set agent=<id>` (CLI) / `arguments.agent` (MCP) / body `agent` (HTTP) mechanism every verb already has — no dedicated `--agent` flag needed. **Race-safety (3a-5):** added `tomlio.atomic_update(path, mutate)` — a read-modify-write that runs entirely inside the *same* lock file `atomic_write` already uses (per the task's own instruction to reuse rather than invent a concurrency mechanism, and Phase 2's note pointing at `test_tomlio.py`'s existing concurrent-writer test). `tickets.set_claim` now does its whole read-check-write inside one `atomic_update` call instead of the previous `load()`+`_save()` pair — that pair only locked the *write* half, so two concurrent claims could both observe an empty `claimed_by` before either wrote and both "succeed" (the exact TOCTOU gap 3a-5 exists to close). A conflicting claim now raises a new `tickets.ClaimConflictError` naming the current holder; `VaultBackend.claim` propagates it unchanged (still thin delegation, only `tickets.set_claim`'s internals changed). **3a-6:** re-claiming by the same identity that already holds it is a no-op success that refreshes `claimed_at`, folded into the same mutate function rather than a separate code path. **3a-7:** `audit.record(repo_root, "ticket.claim", ...)` fires on both success and refusal — a refused claim is exactly the kind of thing worth a line, matching `verb.run`'s own refused-run audit pattern. **Real bug found and fixed while writing 3a-5/3a-8's own concurrency test:** `_acquire_lock`'s retry loop only caught `FileExistsError`, but on Windows, `os.open(lock_path, O_CREAT|O_EXCL)` against a lock file another thread is mid delete-and-recreate on can transiently raise `PermissionError` instead — NTFS delete-then-create is not the single atomic step POSIX unlink+create is. At 4 threads (the pre-existing `test_tomlio.py` writer test) this surfaced only as an occasional `PytestUnhandledThreadExceptionWarning` (documented as a known pre-existing flake at Phase 2 close); at 8 threads (3a-5's own test, matching the actual concurrency this task is validating) it reliably crashed one thread's `set_claim` call outright. Fixed by retrying on `PermissionError` too — a genuine correctness fix to the locking primitive this ticket's own race-safety requirement depends on, not a pre-existing-and-ignored issue.
  - **FR-9 (`comment`, 3a-9..3a-12):** thin wrapper over `backends.default_backend().comment(...)` → `trackers.add(..., "comments", ...)`, which already stamps `author`/`posted_on` (1d-4) — no new attribution/timestamp logic needed here. `audit.record(repo_root, "ticket.comment", ...)` closes the NFR-5 gap (CR-1) the same way 3a-7 does for claims.
  - Both `claim` and `comment` publish to `bus.default()` on success (`ticket://{ticket}`), the same pattern `ticket_move` set in Phase 1's 1e-6/2b-4, so an MCP session subscribed to a ticket learns of a claim or a comment, not just a lane move.
  - Evidence: new `console/tests/test_ready_claim_comment_verbs.py` (23 cases: `ready` exclusion/empty-list, `claim` gates/conflict/idempotent-reclaim/audit/bus/8-way-concurrency-through-the-verb, `comment` attribution/timestamp/never-blocks/audit/bus, one-api registry+MCP-tool-catalogue checks); `console/tests/test_tickets.py::TestClaim` gains 3 cases (conflict refusal, idempotent reclaim, 8-thread concurrent-claim race at the `tickets.set_claim` layer); `console/tests/test_tomlio.py` gains `TestAtomicUpdate` (4 cases: mutate-existing, missing-file-starts-empty, raise-writes-nothing, 4-thread concurrent-increment-loses-nothing). `pytest -o addopts="" console/tests/test_ready_claim_comment_verbs.py console/tests/test_tickets.py console/tests/test_tomlio.py console/tests/test_vault_backend.py console/tests/test_backends.py console/tests/test_mutation_verbs.py -q` → 112 passed (after the Windows lock fix; failed once before it, confirming the fix was necessary, not incidental). Full suite: `pytest -o addopts="" console/tests -q` → **1328 passed** (1298 at Phase 2 close + 30 new: 23 + 3 + 4), stable across 3 repeated runs of the concurrency-heavy `TestClaim` class specifically.
  - No deviation rising to a scope change: the concurrency mechanism (file-lock-guarded read-modify-write, reusing `tomlio`'s existing lock file) is the implementation detail the task instructions explicitly named as a builder's call to make, not a scope question — and race-safety itself was fully delivered, not narrowed.

**Phase 3 complete: all tasks in slice 3a done.** FR-7/FR-8/FR-9/NFR-5 all covered per [[T-017-implementation-plan]]'s AC-coverage table. Stopping here per the per-phase checkpoint policy — Phase 4 (Ops, 12h: stop-hook + `console setup <editor>`) awaits review; not starting it without the harness orchestrator's go-ahead.

Stage: TEMPLATE (Phase 3 of 4 complete, builder checkpoint)

### 2026-09-16 (builder, Phase 4 — final)
- Done: Slice 4a — ready-claim-hooks stop-hook + editor setup [4a-1..4a-8], 4h actual vs 12h estimated.
  - **FR-10 stop-hook (4a-1..4a-3):** grounded in the real hook mechanism first — `.claude/settings.json`'s `Stop` array already runs `console-refresh.sh` (best-effort, `\|\| true`, silent-if-missing); extended that same mechanism with a second script, `.claude/hooks/console-stop-reminder.sh`, rather than inventing a parallel one. New `console/server/stop_hook.py`: `stale_claims(repo_root, agent)` finds tickets `claimed_by == agent` where neither `ticket.toml`'s `updated` field nor any `comments` tracker item's `posted_on` sorts later than `claimed_at` (string comparison is correct even between `claimed_at`'s bare-date form and a full `...T00:00:00Z` timestamp — same day, and a longer string with the date as a strict prefix lexicographically ranks above it, so a same-day comment still counts as "after"). **Identity, reused not invented:** there was no existing per-session "agent id" distinct from `log-work`'s own `knowledge-center/logs/author.local` (line 2, the slug) — `resolve_agent()` reads that file (an explicit `--agent` override still wins), rather than adding a second identity convention. New `kanban.py stop-hook check [--agent A] [--json]` subcommand is the testable seam; the hook script itself is a thin `\|\| true`-wrapped shell wrapper matching `console-refresh.sh`'s own contract (missing `console/kanban.py` or no Python interpreter is silent, exit 0). Registered in `.claude/settings.json`'s `Stop` array alongside the existing refresh hook. Evidence: new `console/tests/test_stop_hook.py` (16 cases: identity override/fallback/absent, stale-via-no-activity, not-stale-via-comment, not-stale-via-move (frozen-date claim + a later move), cross-agent isolation, unclaimed-never-reported, never-raises-on-a-broken-repo-root, CLI `--json`/plain output, never-crashes-even-if-the-check-itself-raises, parser wiring).
  - **FR-11 `console setup <editor>` (4a-4..4a-8):** new `console/server/setup_editor.py` + `kanban.py setup {cursor|claude|vscode} [--json]` (born JSON-compliant per one-api, not added to a later audit list). Grounded in the repo's own root `.mcp.json` (`{"mcpServers": {"console": {"command": "python", "args": ["console/mcp_server.py"]}}}`) as the model config shape FR-11 names. `cursor` writes `.cursor/mcp.json`'s `mcpServers.console` (same shape); `claude` merges into the already-existing root `.mcp.json` rather than writing a second, competing file (Claude Code's own project-scoped MCP config location); `vscode` writes `.vscode/mcp.json`'s `servers.console` with the additional `"type": "stdio"` field that schema names — the concrete key names/paths are the implementation detail the task instructions named as a builder's call, not a scope question. **Idempotency (4a-7), not narrowed:** `_merge_mcp_json` sets only the `"console"` key under each format's servers dict, so a pre-existing sibling server (or any other top-level key in the file) survives a re-run untouched; the `AGENTS.md` snippet is written between two HTML-comment markers and replaced in place on a second run rather than appended again, so pre-existing project content in `AGENTS.md` is preserved and the snippet itself never duplicates. Evidence: new `console/tests/test_setup_editor.py` (13 cases: per-editor config shape, second-run no-op, unrelated-existing-server preserved for `cursor`, merge-into-existing-root-`.mcp.json` for `claude`, `vscode`'s `type: stdio` field, `AGENTS.md` create-when-missing/append-when-present/no-duplicate-marker-on-rerun, unknown-editor `ValueError`, CLI `--json`/plain modes, parser wiring including `choices` rejection of an unknown editor).
  - `console/README.md`'s CLI usage block gains both new subcommands (matching the pattern every other Phase 1-3 slice used for its own new CLI surface).
  - Evidence: `pytest -o addopts="" console/tests/test_stop_hook.py console/tests/test_setup_editor.py -q` → 29 passed; full suite `pytest -o addopts="" console/tests -q` → **1357 passed** (1328 at Phase 3 close + 29 new). Manual end-to-end smoke against the real checkout (not just the test fixture): `python console/kanban.py stop-hook check --json` correctly resolved this session's own identity (`sohail-ali`, from the real `author.local`) and reported no stale claims; `python console/kanban.py setup --help` shows the wired subcommand. Confirmed via `git status` that the smoke test did not touch the real `.mcp.json`/`AGENTS.md` (all mutating tests run against the isolated `tmp_path` fixture, not the real checkout).
  - No deviation rising to a scope change: which editor-config file format/path to use (4a-4/4a-5/4a-6) was named in the task instructions as a builder's own call; idempotency (4a-7) and the `AGENTS.md` snippet (4a-8) were both delivered in full, not narrowed.
- `simplify` pass (single-pass, no Agent-tool fan-out available in this context) over the full Phase 4 diff (`stop_hook.py`, `setup_editor.py`, `kanban.py`'s new imports/cmd functions/parser entries, `console-stop-reminder.sh`, `.claude/settings.json`, `README.md`, `test_stop_hook.py`, `test_setup_editor.py`): no findings — both new modules are single-purpose, share the existing `_die`/`--json`-toggle/never-crash conventions the rest of `kanban.py` already uses, and neither duplicates logic Phases 1-3 already built (`stop_hook.py` reuses `tickets`/`trackers` reads directly rather than going through the Backend SPI, since a workspace-wide claim scan is a read, not one of the SPI's eight mutating-shape methods; `setup_editor.py` has no overlap with `workspace_config.py`, a different "config" entirely). No changes applied beyond what was already written.

**Phase 4 complete: all tasks in slice 4a done.** FR-10/FR-11 covered per [[T-017-implementation-plan]]'s AC-coverage table. **All 4 phases of T-017 are now complete: 58/58 tasks, 1357 tests passing (1237 at Phase 1's mid-point full-suite check → 1357 at close; +120 across the whole ticket).** Actuals across all phases: Phase 1 32h, Phase 2 16.5h, Phase 3 6.5h, Phase 4 4h = **59h actual vs 126.5h estimated** (53% under, consistent with every phase's own pattern of components being smaller once the real code was read). No `evolve`/`replan` was triggered at any point; the one flagged, non-silent scope gap (2c's `workspace.toml` support for a genuinely *renamed* vault/console pair) remains open for a future ticket/evolve, as recorded at Phase 2 close. Not running `verify`/`close-work` — that is the verifier's job next, and a human gate sits before `close-work` regardless. Build is in a clean, fully-committed-artifact state ready for `handoff(builder → verifier)`.

### 2026-09-16 (harness — handoff)
- Handoff gate TEMPLATE → SIMPLIFY → VERIFY: pass. All 58/58 plan tasks `[x]` in task-breakdown.md/plan.md (verified via grep, 0 unchecked boxes); progress.md current; a `simplify` pass was run at the close of every phase (Phase 2, 3, 4 explicitly logged; Phase 1 folded equivalent review into its own task completions) with no unresolved findings — the two issues surfaced (Phase 2's MCP session-id mismatch, Phase 3's Windows `PermissionError` lock race) were both fixed inline in the same slice they were found, not deferred as TODOs.
- Next: `handoff(builder → verifier)`; dispatch verifier for VERIFY (`challenge-implementation` → `verify` → `validate-artifacts`).

Stage: TEMPLATE → SIMPLIFY → VERIFY

### 2026-09-16 (fixer — AC-6 narrowing closed)
- Symptom: verifier's AC-6 passed with a documented narrowing (2c-1's flagged gap, decision-log a7) — a `workspace.toml` naming a genuinely *renamed* vault/console pair (e.g. `vault = "../noble-knowledge"`, the plan's own illustrative example) raised `RepoRootError` instead of resolving, because `find_repo_root` could only reduce a `workspace.toml` to a single `repo_root` string when `vault`/`console` were literally named `knowledge-center`/`console` sharing one parent.
- Root cause: the single-string `repo_root` contract had no way to carry a renamed pair through to the ~25 other modules across the package that still joined `repo_root + "console/..."` / `"knowledge-center/..."` literally.
- Fix: `paths.find_repo_root` now returns a stable anchor (the `workspace.toml`'s own directory) instead of raising for an unreducible pair. New `paths.vault_dir(repo_root)`/`paths.console_dir(repo_root)` re-resolve the real, possibly-renamed directories via `workspace_config.resolve`, falling back to the literal `repo_root/knowledge-center` / `repo_root/console` when no `workspace.toml` governs `repo_root` — a true no-op for every pre-T-017 checkout. New `paths.resolve_rel(repo_root, rel)` lets every `"console/..."`/`"knowledge-center/..."`-shaped relative-path call site switch to the renamed-aware resolvers with a one-line change. Migrated every real call site: `agents.py`, `agent_manager.py`, `agent_backends.py`, `assistant.py`, `assistant_config.py`, `audit.py`, `boards.py`, `jobs.py`, `kickoff.py`, `model_catalog.py`, `multimodal.py`, `native_bridge.py`, `notify.py`, `onboarding.py`, `plugins/registry.py`, `prompt_build.py`, `provider_overrides.py`, `reset.py`, `runs.py`, `schedules.py`, `stop_hook.py`, `telemetry.py`, `tickets.py`, `verbs.py`, `vault.py`, `worklog.py`. Left `harness_lint.py`, `kickoff.py`'s `RENDER_SCRIPT_REL`, and `agents.py`'s `.claude` globs untouched — those are the harness's own source-config location (`.claude/skills`, `.claude/agents`, `CLAUDE.md`), not a vault/console data path a `workspace.toml` renames.
- Verification: replaced `test_paths.py`'s `test_a_layout_workspace_toml_cannot_reduce_to_one_root_raises` (which pinned the exact narrowing being removed) with `test_a_renamed_layout_resolves_via_the_workspace_toml_anchor`; every other pre-existing test unchanged. Added `TestVaultDirConsoleDirRenamedPair` (4 cases in `test_paths.py`, incl. the plan's own illustrative `vault = "../noble-knowledge"` example and a no-workspace.toml no-op check) and a new `console/tests/test_workspace_rename_e2e.py` (4 cases proving `vault.py` and `runs.py` — a vault-side and a console-cache-side module — resolve into the renamed locations end-to-end, not just that `find_repo_root` doesn't crash). Full suite: `pytest -o addopts="" console/tests -q` — before 1357 (1356 passed, 1 failed on the old narrowing test), after **1365 passed, 0 failed** (8 net new tests).
- Updated: `T-017-decision-log.md` (new entry a9, extending a7) and `T-017-task-breakdown.md`'s 2c-1 row (Notes column records the fixer pass and points to a9). `T-017-verification.md` intentionally not touched — the harness orchestrator re-verifies AC-6.
- No scope/requirements shift surfaced: this closes FR-6's own AC completely rather than widening it: no `evolve`/`replan` triggered.
- Next: `handoff` back to verifier to re-check AC-6.

### 2026-09-16 (verifier — AC-6 re-verify + close-work)
- Independent re-derivation of AC-6 (not trusted from fixer narrative): read `console/server/paths.py` as it exists now; ran `pytest -o addopts="" console/tests/test_paths.py console/tests/test_workspace_rename_e2e.py -q` → 14 passed (own run); read `test_workspace_rename_e2e.py`'s body directly and confirmed `vault.list_tree`/`vault.read_file`/`runs.runs_dir`/`runs.create`+`get` genuinely round-trip through a renamed vault/console pair sharing no parent and no literal name match — not just that `find_repo_root` no longer raises. Confirmed backward-compat tests (`TestSiblingFallbackUnchanged`, `test_split_repo_layout_bound_by_workspace_toml_resolves`) unchanged and still passing; only the test pinning the old narrowing bug was replaced, as expected. Full suite: `pytest -o addopts="" console/tests -q` → 1365 passed, 0 failed (own run, matches fixer's reported figure). Updated `T-017-verification.md`'s AC-6 row to full PASS with newly-derived evidence, preserving the narrowing's history for traceability; updated the 11/11 summary line and edge-cases section.
- `handoff(VERIFY → CLOSE)`: gate requires verification.md filled with evidence per criterion (true) plus `verify`(ready) and `validate-artifacts` clean — ran both scopes; found one pre-existing, non-blocking dangling link (`[[T-017-effort-forecast]]` in `T-017-effort-estimate.md`/`T-017-task-breakdown.md` — an optional artifact never generated since no `estimate(mode=forecast)`/replan was triggered; predates this fix and the original VERIFY pass). No new structural/link issues introduced by the AC-6 fix's 26+ file migration. Gate: pass.
- `close-work`: `T-017-verification.md` frontmatter set `status: Complete`, `closed_date: 2026-09-16`; `T-017-summary.md` set `status: Complete`, `tags: [completed]`, `closed_date`, close note appended; board lane synced via `python console/kanban.py ticket move T-017 done` (confirmed `"stage": "done"` in CLI output); `knowledge-center/artifact-map.md` row moved from Active to Completed. Not archived (default). Deployer explicitly not run — separate ASK-gated step per workspace rules.

## Links
- [[T-017-summary]] · [[T-017-analysis]] · [[T-017-requirements]] · [[T-017-decision-log]] · [[T-017-plan]] · [[T-017-progress]] · [[T-017-verification]]
