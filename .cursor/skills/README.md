# `.cursor/skills/`

**Canonical skill definitions live under `.claude/skills/`** (each skill is a folder with `SKILL.md`).

This directory intentionally has **no** `*/SKILL.md` copies so Cursor and Claude Code do not register duplicate skills. Read the same paths from either product. Agent role docs live only under **`.claude/agents/`** (no `.cursor/agents/` copy).
