# Cursor project slot (`control-center`)

**Persistent memory for this harness is shared** at:

`.claude/projects/control-center/memory/`

Do not maintain a separate memory store here; that would fork state when switching between Claude Code and Cursor.
